import React from "react";
import Base from "../core/Base";

const Profiile = () => {
  return (
    <Base title="Prfile page">
      <h1>This is a profile page</h1>
    </Base>
  );
};

export default Profiile;
